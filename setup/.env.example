# Copy this file to .env and fill in real values. .env is gitignored and
# must never be committed.

ITEMS_DB_USER=items
ITEMS_DB_PASSWORD=changeme

USERS_DB_USER=user
USERS_DB_PASSWORD=changeme

AUCTIONS_DB_USER=auctions
AUCTIONS_DB_PASSWORD=changeme

TRANSACTIONS_DB_USER=transaction
TRANSACTIONS_DB_PASSWORD=changeme

WATCHLIST_DB_USER=watchlist
WATCHLIST_DB_PASSWORD=changeme

MESSAGES_MONGO_USER=messagesMongo
MESSAGES_MONGO_PASSWORD=changeme

# Gmail account used to send notification emails. Use an App Password
# (https://myaccount.google.com/apppasswords), never the account password.
EMAIL_ADDRESS=
EMAIL_PASSWORD=
